<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="PIPER.Views.EnvironmentManager"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:PIPER.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <ScrollViewer>
        <Grid Padding="10, 10, 10, 0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            
            <StackPanel Grid.Row="0" >
                <TextBlock Text="Virtual Environment Management" FontSize="26"/>
                <TextBlock/>
                <ComboBox x:Name="comboEnvironments" ItemsSource="{x:Bind PythonEnvironments}"  DisplayMemberPath="Item1" SelectedValuePath="Item2"  SelectionChanged="comboEnvironment_SelectionChanged"
               Header="Current Virtual Environments:"/>
                <TextBlock/>
            </StackPanel>

            <Expander Grid.Row="1" IsExpanded="False" ExpandDirection="Down" Header="Create New Virtual Environment" HorizontalAlignment="Stretch" HorizontalContentAlignment="Stretch">
                <Grid Width="Auto">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*" />
                        <RowDefinition Height="*" />
                        <RowDefinition Height="*" />
                        <RowDefinition Height="*" />
                        <RowDefinition Height="*" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>
                    
                    <TextBox Grid.Row="0" PlaceholderText="Virtual Environment Name"/>
                    <TextBlock Grid.Row="1"/>
                    <ComboBox Grid.Row="2" ItemsSource="{x:Bind PythonVersions}"  DisplayMemberPath="Item1" SelectedValuePath="Item2"  SelectionChanged="comboPythonVersions_SelectionChanged"
                        Header="Python Version:"/>
                    <TextBlock Grid.Row="3"/>
                    <StackPanel Grid.Row="4" Orientation="Horizontal" Spacing="8">
                        <CheckBox Content="Install Juypter" x:Name="checkbox_InstallJuypter" IsChecked="True"/>
                        <CheckBox  Content="Install Git" x:Name="checkbox_InstallGit" IsChecked="True"/>
                    </StackPanel>
                    
                    <TextBlock Grid.Row="5"/>
                    <Button Grid.Row="6" Content="Create" Click="Create_Button_Click" Style="{StaticResource AccentButtonStyle}"/>
                </Grid>
            </Expander>

            <StackPanel Grid.Row="2">
                <TextBlock/>
                <TextBox Header="Output:" AutomationProperties.Name="multi-line TextBox" TextWrapping="Wrap" AcceptsReturn="True" HorizontalAlignment="Stretch" MaxHeight="500"/>
            </StackPanel>
           
            <!-- Bottom Text -->
            <TextBlock Grid.Row="3"
                   Text="{Binding FooterText}"
                   HorizontalAlignment="Center"
                   Margin="0,30,0,10"
                   FontSize="14"
                   Foreground="Gray"/>
        </Grid>
    </ScrollViewer>
</Page>
