<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="PIPER.Views.Settings"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:PIPER.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <Grid Padding="10,10,10,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <TextBlock Text="Settings" FontSize="26" Grid.Row="0"/>

            <TextBox Header="Installer Location:" PlaceholderText="Local Installer Directory" x:Name="InstallerDirectory" Grid.Row="1" Margin="0,10,0,0"/>
            <TextBox Header="Virtual Environment Location:" PlaceholderText="Local Environment Directory" x:Name="EnvironmentDirectory" Grid.Row="2" Margin="0,10,0,0"/>
            <TextBox Header="Jupyter Kernel Location:" PlaceholderText="Local Kernel Directory" x:Name="KernelDirectory" Grid.Row="3" Margin="0,10,0,0"/>
            <TextBox Header="Python Download URL:" PlaceholderText="Python Download URL" x:Name="PythonURL" Grid.Row="4" Margin="0,10,0,0"/>

            <Expander IsExpanded="False" ExpandDirection="Down" Header="Proxies" Grid.Row="5" Margin="0,10,0,0" HorizontalAlignment="Stretch" HorizontalContentAlignment="Stretch">
                <Grid Width="Auto">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="10"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <TextBox x:Name="HTTPURL" Header="Proxy URL:" PlaceholderText="HTTP Proxy URL"
                         Grid.Row="0" HorizontalAlignment="Stretch" MinWidth="200" Width="Auto"/>
                </Grid>
            </Expander>

            <StackPanel Orientation="Horizontal" Spacing="8" Grid.Row="6" Margin="0,20,0,0">
                <Button Content="Save" Click="Save_Button_Click" Style="{StaticResource AccentButtonStyle}"/>
                <Button Content="Reset" Click="Reset_Button_Click"/>
            </StackPanel>
        </Grid>
    </ScrollViewer>
</Page>
